# macOS
.DS_Store

# graphify knowledge graph (derived artifact; rebuild with `graphify extract .`)
graphify-out/

# Per-machine Claude Code settings. Holds two things that are per-developer:
# graphify's PreToolUse hooks (absolute path to the graphify executable) and
# the auto-draft-PR Stop hook. The script it calls (.claude/hooks/) is
# committed; enabling it is per-developer.
.claude/settings.local.json

# Node. Two workspaces, both of them ADR-035 §2.10 suites: `packages/money` (the
# paired-arithmetic half) and `supabase/vitest` (concurrency, two connections).
node_modules/
vitest-report.json
vitest-db-report.json
